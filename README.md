# نظام إدارة الدروس الخصوصية
# Private Tutor Management System

نظام شامل لإدارة الدروس الخصوصية مبني باستخدام React + Vite و Supabase

## 🎯 المميزات الرئيسية

### 1. إدارة الطلاب
- إضافة طلاب جدد
- عرض قائمة الطلاب
- حذف الطلاب مع تأكيد SweetAlert2

### 2. جدولة الدروس (مع كشف التعارضات)
- اختيار الطالب واليوم (الأحد-السبت)
- تحديد وقت البداية والنهاية بدقة الدقيقة
- **منطق كشف التعارضات**: يمنع جدولة حصتين متداخلتين في نفس اليوم
  - الصيغة: `(NewStart < ExistingEnd) AND (NewEnd > ExistingStart)`
- رسالة خطأ واضحة عند وجود تعارض

### 3. الحصص اليومية
- عرض تلقائي لحصص "اليوم"
- حساب يوم الأسبوع من التاريخ
- عرض جميع الحصص المجدولة لليوم المحدد
- تسجيل الحضور السريع

### 4. الحضور والسجلات
- عرض السجلات حسب التاريخ
- أعمدة: اسم الطالب، الحالة، الإجراءات
- **تعديل الحالة**: تغيير من "غائب" إلى "حاضر" وغيره
- حذف السجلات مع تأكيد
- إحصائيات يومية

### 5. لوحة التحكم
- عدد الطلاب النشطين
- عدد الحصص اليوم
- حضور هذا الأسبوع

## 🗄️ هيكلة قاعدة البيانات

### الجداول:
1. **students**: `id`, `name`, `created_at`
2. **schedules**: `id`, `student_id` (FK), `day_of_week` (0-6), `start_time`, `end_time`
3. **attendance**: `id`, `student_id` (FK), `date`, `status` (enum: present, absent, excused, postponed)

---

## 📦 التثبيت والإعداد

### الخطوة 1️⃣: تثبيت المكتبات

افتح Terminal في مجلد المشروع وقم بتشغيل:

```powershell
npm install
```

### الخطوة 2️⃣: إعداد قاعدة البيانات في Supabase

1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. أنشئ مشروع جديد أو افتح مشروع موجود
3. اذهب إلى **SQL Editor**
4. انسخ محتوى ملف `supabase_schema.sql` بالكامل
5. الصق المحتوى في محرر SQL وشغّل الاستعلام (Run)

### الخطوة 3️⃣: إعداد متغيرات البيئة

1. انسخ ملف `.env.example` إلى `.env`:
```powershell
Copy-Item .env.example .env
```

2. افتح ملف `.env` وأدخل معلومات Supabase:
```
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

**للحصول على المعلومات:**
- اذهب إلى Supabase Dashboard → Settings → API
- انسخ "Project URL" و "anon/public key"

### الخطوة 4️⃣: تشغيل المشروع

```powershell
npm run dev
```

سيفتح المشروع تلقائياً على: `http://localhost:3000`

---

## 🏗️ بناء المشروع للإنتاج

```powershell
npm run build
```

## 📁 هيكل الملفات

```
tutor-management-system/
├── supabase_schema.sql          # سكريبت قاعدة البيانات
├── package.json                 # المكتبات والنصوص
├── vite.config.js              # إعدادات Vite
├── index.html                  # صفحة HTML الرئيسية
├── .env.example                # مثال لمتغيرات البيئة
├── .gitignore                  # ملفات محجوبة عن Git
└── src/
    ├── main.jsx                # نقطة الدخول
    ├── App.jsx                 # التوجيه والتنقل
    ├── supabaseClient.js       # إعداد Supabase
    └── pages/
        ├── Dashboard.jsx       # لوحة التحكم
        ├── Students.jsx        # إدارة الطلاب
        ├── Schedule.jsx        # جدولة الدروس (مع كشف التعارضات)
        ├── DailyClasses.jsx    # الحصص اليومية
        └── Attendance.jsx      # الحضور والسجلات
```

## 🛠️ التقنيات المستخدمة

- **React 18** - مكتبة واجهة المستخدم
- **Vite** - أداة البناء السريعة
- **React Router DOM** - التوجيه
- **Supabase** - قاعدة البيانات (PostgreSQL)
- **Bootstrap 5 RTL** - التصميم (مع دعم العربية)
- **SweetAlert2** - التنبيهات والتأكيدات

## ⚡ ملاحظات مهمة

### منطق كشف التعارضات (CRITICAL LOGIC)

في صفحة جدولة الدروس، يتم التحقق من التعارضات قبل حفظ الحصة:

```javascript
// الصيغة: إذا كان الوقت الجديد يتداخل مع وقت موجود
if (newStart < existingEnd && newEnd > existingStart) {
  // يوجد تعارض - منع الحفظ
}
```

### أنواع حالات الحضور

- `present` (حاضر) ✅
- `absent` (غائب) ❌
- `excused` (غياب بعذر) 📝
- `postponed` (مؤجل) ⏰

## 🚀 الاستخدام

1. **ابدأ بإضافة الطلاب** في صفحة "الطلاب"
2. **جدول الحصص** في صفحة "جدولة الدروس"
3. **سجل الحضور** يومياً في "الحصص اليومية"
4. **راجع السجلات** في "الحضور والسجلات"

## 📝 الدعم

في حالة وجود مشاكل:
- تأكد من تشغيل سكريبت `supabase_schema.sql`
- تحقق من صحة معلومات `.env`
- راجع Console في المتصفح للأخطاء

---

**تم بناؤه بـ ❤️ باستخدام React + Supabase**
