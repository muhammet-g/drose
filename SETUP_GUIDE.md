# 🚀 دليل التشغيل السريع
# Quick Start Guide

## ✅ تم إنشاء المشروع بنجاح!

تم إنشاء جميع الملفات وتثبيت المكتبات. اتبع الخطوات التالية لبدء التشغيل:

---

## 📋 الخطوات المطلوبة

### ⚡ الخطوة 1: إعداد قاعدة البيانات في Supabase

1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. سجل دخول أو أنشئ حساب جديد
3. أنشئ مشروع جديد (New Project)
4. بعد إنشاء المشروع، اذهب إلى **SQL Editor** في القائمة الجانبية
5. انسخ **محتوى ملف** `supabase_schema.sql` بالكامل
6. الصق المحتوى في محرر SQL
7. اضغط على زر **Run** أو **F5** لتنفيذ الاستعلام

✅ سيتم إنشاء 3 جداول: students, schedules, attendance

---

### ⚡ الخطوة 2: إعداد ملف `.env`

1. الملف `.env` موجود بالفعل في المجلد
2. افتح الملف `.env` في VS Code
3. احصل على معلومات الاتصال من Supabase:
   - اذهب إلى **Settings** → **API** في لوحة تحكم Supabase
   - انسخ **Project URL**
   - انسخ **anon/public key**

4. أدخل المعلومات في ملف `.env`:

```env
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

⚠️ **مهم جداً**: تأكد من حفظ الملف بعد التعديل!

---

### ⚡ الخطوة 3: تشغيل المشروع

في Terminal (PowerShell)، شغّل الأمر:

```powershell
npm run dev
```

سيفتح المتصفح تلقائياً على: `http://localhost:3000`

---

## 🎉 ابدأ الاستخدام!

بعد فتح المشروع، اتبع هذا التسلسل:

1. **صفحة الطلاب**: أضف طلاب جدد
2. **جدولة الدروس**: أنشئ جدول الحصص الأسبوعية
3. **الحصص اليومية**: سجل الحضور يومياً
4. **الحضور والسجلات**: راجع وعدّل السجلات

---

## 🔍 التحقق من صحة الإعداد

### اختبار الاتصال بقاعدة البيانات:

1. شغّل المشروع (`npm run dev`)
2. افتح المتصفح
3. اذهب إلى صفحة "الطلاب"
4. حاول إضافة طالب جديد

**إذا ظهرت رسالة نجاح** ✅ → الإعداد صحيح!

**إذا ظهر خطأ** ❌ → راجع:
- هل شغّلت سكريبت SQL في Supabase؟
- هل المعلومات في `.env` صحيحة؟
- هل حفظت ملف `.env` بعد التعديل؟
- افتح Console في المتصفح (F12) لرؤية الأخطاء التفصيلية

---

## 📝 ملفات المشروع الرئيسية

```
✅ supabase_schema.sql       → سكريبت قاعدة البيانات (نفذه في Supabase أولاً)
✅ .env                      → معلومات الاتصال (عدّل هذا الملف)
✅ package.json              → المكتبات (مثبتة بالفعل)
✅ src/App.jsx               → التطبيق الرئيسي
✅ src/pages/                → صفحات التطبيق
```

---

## 🛑 استكشاف الأخطاء

### المشكلة: "Missing Supabase credentials"
**الحل**: تأكد من ملف `.env` وأعد تشغيل `npm run dev`

### المشكلة: خطأ عند إضافة طالب
**الحل**: تحقق من أنك نفذت `supabase_schema.sql` في Supabase SQL Editor

### المشكلة: الصفحة فارغة
**الحل**: افتح Console (F12) واقرأ رسالة الخطأ

---

## 📞 الأوامر المفيدة

```powershell
# تشغيل المشروع في وضع التطوير
npm run dev

# بناء المشروع للإنتاج
npm run build

# معاينة النسخة المبنية
npm run preview

# إعادة تثبيت المكتبات (إذا حدثت مشاكل)
Remove-Item -Recurse -Force node_modules
npm install
```

---

## ✨ مميزات خاصة

### 🔒 كشف تعارض المواعيد
النظام يمنع جدولة حصتين متداخلتين في نفس اليوم تلقائياً!

### 🕐 دقة الدقيقة
يمكنك اختيار أي وقت بدقة الدقيقة (مثل: 7:03 مساءً)

### 🔄 تعديل مباشر
يمكن تعديل حالة الحضور من "غائب" إلى "حاضر" مباشرةً

### 📊 إحصائيات تلقائية
لوحة التحكم تحسب الإحصائيات تلقائياً من قاعدة البيانات

---

**جاهز للاستخدام! 🎓📚**
